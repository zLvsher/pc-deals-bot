# --- Core runtime ---
fastapi>=0.115            # Web API + WebSocket server (interfaccia grafica locale)
uvicorn[standard]>=0.30   # ASGI server
pydantic>=2.7             # Modelli di dati e validazione parametri
httpx>=0.27               # Client HTTP asincrono per gli scraper

# --- Parsing / scraping ---
beautifulsoup4>=4.12      # Parsing HTML (fallback sui siti che non hanno JSON)
lxml>=5.2                 # Parser veloce per BeautifulSoup

# --- Scheduling ---
apscheduler>=3.10         # Scheduler del loop periodico del bot (intervallo configurabile)

# --- Notifiche (fase 2, già predisposte) ---
python-telegram-bot>=21   # Alert Telegram (opzionali, si attivano con BOT_TOKEN)
websockets>=12            # Push in tempo reale verso la UI

# --- Dev / test ---
pytest>=8.2
pytest-asyncio>=0.23
